var appControllers = angular.module('appControllers',[]);
	appControllers.controller('MainCtrl',['$scope',function ($scope) {
		
	}]);

	appControllers.controller('ProfileCtrl',['$scope',function ($scope) {
		
	}]);

	appControllers.controller('AboutCtrl',['$scope',function ($scope) {
		
	}]);

	appControllers.controller('PortfolyoCtrl',['$scope',function ($scope) {
		
	}]);

	appControllers.controller('IletisimCtrl',['$scope',function ($scope) {
		
	}]);